{#
/**
 * @file
 * Theme override to display a single page.
 *
 * The doctype, html, head and body tags are not in this template. Instead they
 * can be found in the html.html.twig template in this directory.
 *
 * Available variables:
 *
 * General utility variables:
 * - base_path: The base URL path of the Drupal installation. Will usually be
 *   "/" unless you have installed Drupal in a sub-directory.
 * - is_front: A flag indicating if the current page is the front page.
 * - logged_in: A flag indicating if the user is registered and signed in.
 * - is_admin: A flag indicating if the user has permission to access
 *   administration pages.
 *
 * Site identity:
 * - front_page: The URL of the front page. Use this instead of base_path when
 *   linking to the front page. This includes the language domain or prefix.
 *
 * Page content (in order of occurrence in the default page.html.twig):
 * - messages: Status and error messages. Should be displayed prominently.
 * - node: Fully loaded node, if there is an automatically-loaded node
 *   associated with the page and the node ID is the second argument in the
 *   page's path (e.g. node/12345 and node/12345/revisions, but not
 *   comment/reply/12345).
 *
 * Regions:
 * - page.before_header: Items for the before header region.
 * - page.menu: Items for the menu region.
 * - page.header: Items for the header region.
 * - page.breadcrumb: Items for the breadcrumb region.
 * - page.search: Items for the search region.
 * - page.languages: Items for the languages region.
 * - page.after_header: Items for the after_header region.
 * - page.highlighted: Items for the highlighted content region.
 * - page.help: Dynamic help text, mostly for admin pages.
 * - page.before_content: The before content of the current page.
 * - page.sidebar_first: The sidebar.
 * - page.content: The main content of the current page.
 * - page.after_content: The after content of the current page.
 * - page.before_footer: The before footer.
 * - page.footer: Items for the footer region.
 * - page.after_footer: The after footer.
 *
 * @see template_preprocess_page()
 * @see html.html.twig
 */
#}
{% set layout_container_attributes =
  merge_html_attributes(
    layout_container_attr ?? null,
    {},
    {
      class: 'layout-container'
    }
  )
%}

{% set layout_content_attributes =
  merge_html_attributes(
    layout_content_attr ?? null,
    {
      class: 'flex'
    },
    {
      class: 'layout-content'
    }
  )
%}

{% set sidebar_first_attributes =
  merge_html_attributes(
    sidebar_first_attr ?? null,
    {},
    {
      class: 'sidebar-first'
    }
  )
%}

{% set sidebar_sticky_attributes =
  merge_html_attributes(
    sidebar_sticky_attr ?? null,
    {
      class: 'sticky top-4'
    },
    {
      class: 'sidebar-sticky'
    }
  )
%}

{% set content_attributes =
  merge_html_attributes(
    content_attr ?? null,
    {},
    {
      class: 'content w-full'
    }
  )
%}

<div {{ html_attributes(layout_container_attributes) }}>
  {{ page.before_header }}
  {% block menu %}
    {{ page.menu }}
  {% endblock %}
  {{ page.header }}

  {{ page.breadcrumb }}
  {{ page.search }}
  {{ page.languages }}
  {{ page.after_header }}

  {{ page.highlighted }}

  {{ page.help }}

  <main role="main">
    <a id="main-content" tabindex="-1"></a>{# link is in html.html.twig #}
    {{ page.before_content }}

    <div {{ html_attributes(layout_content_attributes) }}>
      {% if page.sidebar_first %}
        <div {{ html_attributes(sidebar_first_attributes) }}>
          <div {{ html_attributes(sidebar_sticky_attributes) }}>
            {{ page.sidebar_first }}
          </div>
        </div>
      {% endif %}
      <div {{ html_attributes(content_attributes) }}>
        {{ page.content }}
      </div>
    </div>
    {# /.layout-content #}

    {{ page.after_content }}
  </main>

  {% if page.before_footer or page.footer or page.after_footer %}
    <footer role="contentinfo">
      {{ page.before_footer }}
      {{ page.footer }}
      {{ page.after_footer }}
    </footer>
  {% endif %}
</div>
{# /.layout-container #}
